import LandingMobile from "@/pages/LandingMobile";

// [PRUEBA] Rediseño mobile-first (mockup). En esta rama la landing renderiza el
// diseño nuevo para poder probarlo en un preview de Vercel sin tocar prod.
const Index = () => <LandingMobile />;

export default Index;
